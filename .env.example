# Copy this file to ".env.local" and fill in the values.
# Never commit secrets to version control.

# ─── Engine Selection (required) ─────────────────────────────────────
# STORAGE_ENGINE: "s3" (object storage) or "sqlite" (local dev)
# AUTH_ENGINE: "dev" (fake HMAC login) or "google" (Google OAuth)
STORAGE_ENGINE="sqlite"
AUTH_ENGINE="google"

# ─── Secrets ────────────────────────────────────────────────────────

# HMAC secret for signing session cookies (min 32 chars)
# Generate: openssl rand -hex 32
COOKIE_SECRET=""

# Random secret for Argon2id user-ID mangling salt
# Generate: openssl rand -hex 32
HMAC_KEY=""

# ─── Google OAuth (required when AUTH_ENGINE=google) ────────────────
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create an OAuth 2.0 Client ID (type: Web application)
# 3. Add authorized redirect URIs:
#      - http://localhost:3000/api/auth/callback/google   (local dev)
#      - https://<your-vercel-domain>/api/auth/callback/google   (production)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Random secret for next-auth JWT signing
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=""

# ─── S3 Storage (required when STORAGE_ENGINE=s3) ───────────────────
# Any S3-compatible provider works (Cloudflare R2, AWS S3, MinIO, etc.)
#
# Example: Cloudflare R2
# 1. Sign up at https://dash.cloudflare.com
# 2. Go to R2 → Create bucket (e.g. "fgt-users")
# 3. Go to R2 → Manage R2 API Tokens → Create API token
#    - Permissions: Object Read & Write
#    - Specify the bucket you created
# 4. Copy the token's Access Key ID and Secret Access Key below
# 5. Endpoint format: https://<account-id>.r2.cloudflarestorage.com
S3_ENDPOINT=""
S3_REGION=""
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
S3_BUCKET_NAME="fgt-users"

# ─── Allow-list (GitHub Gist) ───────────────────────────────────────
# Required when AUTH_ENGINE=google. Build-time fetch of allowed user IDs.
# 1. Create a GitHub Gist with one mangled user ID per line
# 2. Copy the raw URL: https://gist.githubusercontent.com/<user>/<gist>/raw/...
ALLOW_LIST_GIST_URL=""

# ─── Language ────────────────────────────────────────────────────────
# Build-time language selection: "fr" (default) or "en"
# NEXT_PUBLIC_LANG="fr"
